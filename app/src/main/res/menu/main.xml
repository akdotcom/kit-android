<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.lastinitial.stitch.MainActivity" >

    <item
        android:id="@+id/menu_item_share"
        android:showAsAction="always"
        android:orderInCategory="100"
        android:title="@string/menu_share"
        android:icon="@android:drawable/ic_menu_share"
        />
        <!--This is how we're supposed to do it, but I don't like the stupid outlines-->
        <!--around the icons this causes, so explicitly specifying the provider class-->
        <!--in the Activity which seems to work-->
        <!--android:actionProviderClass=-->
            <!--"android.widget.ShareActionProvider"-->

    <item android:id="@+id/action_feedback"
        android:title="@string/menu_feedback"
        android:orderInCategory="100"
        android:showAsAction="never" />

    <item android:id="@+id/action_run_notifications"
        android:title="@string/menu_run_notifications"
        android:orderInCategory="100"
        android:showAsAction="never" />
</menu>
